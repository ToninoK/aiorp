[project]
name = "proxy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["aiorp>=0.1.0a2", "pyjwt>=2.10.1", "aiohttp>=3.9.0"]

[tool.pyright]
venvPath = "./"
venv = ".venv"
